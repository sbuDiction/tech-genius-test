import { AppBar, Toolbar } from "@mui/material";

export const Header = ({}) => {
    return (
        <>
            <header>
                <AppBar>
                    <Toolbar>HR Adminstration System</Toolbar>
                </AppBar>
            </header>
        </>
    )
}